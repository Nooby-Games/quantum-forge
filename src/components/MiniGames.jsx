import React from 'react';
export default function MiniGames(){ return (<div className='panel'><h2>Mini-Games</h2><div style={{display:'flex',gap:8}}><button onClick={()=>{ const n=parseInt(prompt('Enter successful clicks')||'0',10); const k='qf_final_v1'; const s=JSON.parse(localStorage.getItem(k)||'{}'); s.ore=(s.ore||0)+Math.floor(n/4); localStorage.setItem(k,JSON.stringify(s)); alert('Reward granted'); location.reload(); }}>Click Challenge</button><button onClick={()=>{ const t=parseFloat(prompt('Enter timing (s)')||'0'); const diff=Math.abs(t-3); const reward=Math.max(0,Math.floor(6-diff*2)); const k='qf_final_v1'; const s=JSON.parse(localStorage.getItem(k)||'{}'); s.essence=(s.essence||0)+reward; localStorage.setItem(k,JSON.stringify(s)); alert('Gained '+reward+' essence'); location.reload(); }}>Timing</button></div></div>); }