import React from 'react';
export default function Prestige(){ function rebirth(){ const key='qf_final_v1'; const raw=localStorage.getItem(key); if(!raw) return alert('Load'); const s=JSON.parse(raw); if((s.ore||0) < 10000) return alert('Need 10k ore'); s.prestige = s.prestige || {rebirths:0}; s.prestige.rebirths += Math.floor((s.ore||0)/10000); s.ore = 0; s.perClick = 1 + (s.prestige.rebirths * 0.05); s.upgrades = {}; localStorage.setItem(key,JSON.stringify(s)); alert('Rebirthed'); location.reload(); } return (<div className='panel'><h3>Prestige</h3><div style={{fontSize:13}}>Rebirth at 10k ore.</div><div style={{marginTop:8}}><button onClick={rebirth}>Rebirth</button></div></div>); }