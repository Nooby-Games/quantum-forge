import React from 'react';
export default function Automation(){ function buyAuto(){ const key='qf_final_v1'; const raw=localStorage.getItem(key); if(!raw) return alert('Load'); const s=JSON.parse(raw); if((s.ore||0) < 500) return alert('Need 500 ore'); s.ore -= 500; s.perSecond = (s.perSecond||0) + 5; localStorage.setItem(key,JSON.stringify(s)); alert('Bought auto'); location.reload(); } return (<div className='panel'><h3>Automation</h3><div style={{fontSize:13}}>Buy automations.</div><div style={{marginTop:8}}><button onClick={buyAuto}>Buy Auto (500)</button></div></div>); }